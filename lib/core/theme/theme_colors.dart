import 'package:flutter/material.dart';

class ThemeColors{
  const ThemeColors();

  static Color cadet = Color(0xff5d737e);
  static Color greenSheen = Color(0xff64b6ac);
  static Color richBlack = Color(0xff000500);
  static Color lincolnGreen = Color(0xff036016);
  static Color carnelian = Color(0xffba2422);

  static Color primarySwatch = Colors.teal;
  static Color backgroundColor = Colors.pinkAccent;
  static Color cardColor = Colors.teal;
  static Color accentColor = Colors.white;
}